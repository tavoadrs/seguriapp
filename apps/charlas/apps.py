from django.apps import AppConfig


class CharlasConfig(AppConfig):
    # Aquí es donde se define la etiqueta (label)
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.charlas'
